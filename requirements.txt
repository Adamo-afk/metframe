# Core ML / fine-tuning
#
# IMPORTANT: PyTorch must be installed with CUDA support. The default
# 'pip install torch' installs CPU-only, which will fail for QLoRA
# training, HF inference, and BERTScore (all run on GPU VRAM).
#
# Install PyTorch with CUDA 12.6 FIRST, before installing the rest:
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# Then install the remaining dependencies:
#
#   pip install -r requirements.txt
#
# The torch line below is commented out to prevent pip from overwriting
# the CUDA build with a CPU-only build. If you already have CUDA torch
# installed, pip will skip it when processing this file.
#
# torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
trl>=0.8.0
bitsandbytes>=0.43.0

# Evaluation metrics
rouge-score>=0.1.2
bert-score>=0.3.13
nltk>=3.8.0

# API clients
openai>=1.30.0
requests>=2.31.0

# Data processing
pandas>=2.0.0
pdfplumber>=0.10.0

# Visualization (used by generate_metric_plots.py, generate_plots_llm_as_a_judge.py)
matplotlib>=3.7.0
seaborn>=0.12.0

# Model selection GUI
beautifulsoup4>=4.12.0
psutil>=5.9.0
GPUtil>=1.4.0

# ---------------------------------------------------------------------------
# System dependencies (not installable via pip):
#
# Ollama >= 0.11.0
#   The inference pipeline calls Ollama's REST API at localhost:11434
#   via the requests library. Install Ollama from https://ollama.com
#   and ensure it is running before using --test_models or --download_models.
#   The ollama Python package is NOT used; all communication is via HTTP.
# ---------------------------------------------------------------------------
